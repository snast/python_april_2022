# States, Cities
# 1 to many
# 1 - State(Washington)
# Many - Seattle, Tacoma, Vancouver
# you need a primary key, (do not forget to check off AI)
# created_at and updated_at DATETIME field (remember you can use the default values NOW(), NOW() ON UPDATE NOW())